apiVersion: v1
kind: Service
metadata:
  name: drive
  labels:
    app.kubernetes.io/name: drive
    app.kubernetes.io/instance: drive
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: drive
    app.kubernetes.io/instance: drive
